---
title: Portfolio
date: 2026-06-12
tags: [project, portfolio, career, website]
---

# 🌍 Portfolio

Index note for the **portfolio website** project — the bridge to the next chapter.

## The goal

**Updated 2026-07-11 — two stages.** Stage 1 (now): land a better-paying **local creative job** (Barcelona area / Sant Cugat, or Madrid) — creative agencies, production houses, marketing — to earn and save. Stage 2 (mid-/long-term): the original dream, a **creative job abroad** (Paris, Amsterdam, or anywhere with better professional outcomes than Spain), funded by Stage 1. The original Dec 2026 – Feb 2027 window now applies to landing the *local* job.

The blocker isn't experience — it's **visibility**. The work exists (shipped apps, cinema background, photography, soon a fashion brand) but lives nowhere anyone can see it. The portfolio fixes that.

## What it is

A **personal website** that shows work, not credentials. English is the primary version; a full **Spanish mirror lives under `/es/`** since 2026-08-18 (see the bilingual entry in the Roadmap), for the local Barcelona/Madrid track. Four sections:

1. **Apps** — the 2 apps live on the App Store are the strongest proof: *"I shipped real products."* Doppio, Warket and Village join as they're finished.
2. **Photography** — a curated selection (10–20 best shots, not everything).
3. **Projects** — cinema background work + the fashion brand once it launches (~Aug–Sep 2026, a centerpiece: brand identity, product, photos).
4. **Bio / About** — short, in English, telling the story *cinema → apps → brand*. The unusual mix is the angle, not a weakness.

## Principles

- **Free hosting** (GitHub Pages or similar) — no monthly cost.
- **English first** — written for the cities we're aiming at. Spanish is a full mirror, not a rewrite: same structure, same work, translated copy (since 2026-08-18).
- **Show, don't list** — every section is work you can look at, not bullet points.
- It should feel *designed* — the site itself is a portfolio piece.

## Timeline

| When | Milestone |
|---|---|
| Month 1 (~July 2026) | Site live with apps + photography + bio |
| Month 3 (~Sep 2026) | Fashion brand section added at launch |
| Months 4–8 | Apply with a real portfolio, well before the deadline |

## Roadmap

- [x] **Site structure** — one-page scroll, see [[Projects/Portfolio/site-structure|site-structure]]
- [x] **Bio / About** — draft v2 approved 2026-06-13, see [[Projects/Portfolio/bio|bio]]
- [x] **Hero repositioned** 2026-07-11 — from personal intro to professional candidacy (Product design · UX · Photography, "story and image" line, AI as headline skill, fashion brand removed). Final copy + decisions in [[Projects/Portfolio/bio|bio]]
- [x] **Hero precision pass** 2026-07-11 — name scales to 8.5rem desktop, vermilion middots in the kicker (the hero's one accent, echoing nav Contact), tightened spacing rhythm, slightly shallower top padding so the work stays near the fold. Composition unchanged (kept quiet per [[Projects/Portfolio/design-principles|design-principles]]; "film credits" hero idea considered and declined)
- [x] **Hero middot field** 2026-07-11, v2 2026-07-12, v3 2026-07-13 — the site's one interactive effect: a dot grid, invisible at rest; dots near the pointer wake in vermilion and settle back when it leaves. v1 was rejected as too subtle and column-bound; v2 is full-bleed (edge to edge), brighter, with a longer settle, and adds a **legibility mask**: dots under the hero text barely wake, so the effect decorates around the content (user rule: effects never compete with text). Chosen over "darkroom develop" and "viewfinder" concepts for exactly that reason. **v3: the field no longer stops at the hero** — it dissolves into the Apps section (dots get scarcer and softer row by row, a halftone-style grain fade, gone partway into the app grid), so the effect introduces the work instead of ending at a rigid edge. The wake also follows the pointer while scrolling, awake dots lean ~2px toward the cursor, and the apps' section head / screenshots / metadata joined the legibility mask. Passes [[Projects/Portfolio/design-principles|design-principles]] because nothing moves until the visitor does (the "still at rest" test recorded there). Pointer-only: touch screens and reduced-motion visitors get the plain hero.
- [ ] **Gather raw material** ← current step (user's task)
	- [x] App info: **Culla** (live: https://apps.apple.com/us/app/culla/id6761316914) and **CullaMusic** (pending Apple review). Both live at **culla.app**. Screenshots gathered and wired in for Culla, CullaMusic and Doppio (verified 2026-07-27).
	- [ ] Photography: 10–20 best shots, user gathering them, ETA ~**2026-06-19**
	- [ ] Projects (pending): short film as **DOP**, distributed on **MUBI**: https://mubi.com/es/es/films/mia-marc, plus 2 more pieces to gather
- [x] **CV section zoned** 2026-07-11 — #cv is sent directly to recruiters, so it now reads as a distinct document: full-bleed band one paper-tone deeper (new `--paper-2` token, both themes, no divider lines) + two-column desktop layout (Experience left, Education/Skills/Languages/Interests right) so the whole CV reads at a glance. **About integrated into the band** (opens the document: portrait + story, then the structured CV below); the separate About nav link removed, `#about` anchor kept working for old links. **Superseded 2026-08-08**: the CV moved to its own page, see below
- [x] **CV moved to /cv/, home slimmed** 2026-08-08 (final step of the editorial redesign) — the full CV is now a standalone page at **/cv/** with a letterhead (name, Barcelona, email, phone), a "Download PDF" button, and **@media print styles** so Print to PDF yields a clean two-column A4 document (recruiter-ready; old /#cv links forward there via a JS redirect). Home now reads **name → line → Photography (curated) → Apps → Projects → Info → contact**: Photography moved above Apps (the middot field retargeted to dissolve into whichever section leads), the CV band replaced by a short **Info** block (portrait + 3-line bio + Full CV link, #about anchor kept), and the footer slimmed to email + App Store link (GitHub left in a comment; **phone now lives only on the CV page**). Projects kept, read as part of the apps/studio cluster
- [x] **Copy audit: self-evident text removed** 2026-07-11 — cut all "site explaining itself" copy: under-construction promises ("more on the way" ×2), the fashion-brand tease in the Projects note, "Live on the App Store" ×3 (badge already says it), the CV section note, the Leica title decoration, plus em-dash and "coded" violations. App descriptions now say what each app does (Culla: photo clean-up by swipe; CullaMusic: Apple Music sorting by swipe; Doppio: living metronome)
- [x] **Site typeface decided** 2026-07-11 — **SF Mono** (JetBrains Mono fallback for non-Apple), chosen via a 21-font cycle test on the live hero; test apparatus removed after the decision. **Superseded 2026-08-08** by the editorial restyle below. Details in [[Projects/Portfolio/design-principles|design-principles]]
- [x] **Editorial restyle** 2026-08-08 — moved the site toward a minimal editorial art-portfolio feel. Type: **Fraunces** (display serif: name + section titles only, weight 350) + **Inter** (everything else), replacing SF Mono. Color: near-monochrome token palette (`--ink/--ink-soft/--paper/--paper-warm/--line`), **vermilion accent retired** — photographs are now the page's only color; the middot field wakes in ink. Spacing: airier section rhythm (~10vh per side), body measure 62ch. The brief's "thin 1px rules between sections" were implemented, then **rejected by the user same day** — the 2026-06-13 no-hairlines rule stands permanently; separation is whitespace only. Details in [[Projects/Portfolio/design-principles|design-principles]]
- [x] **Photography restructured** 2026-08-08 — the home section is now a **curated selection of 6 slots** in a variable rhythm (wide piece, two-up pair, wide piece, pair) instead of the 14-photo masonry grid; current picks are placeholders, **user still to choose the final 6** (all filenames listed in an HTML comment in index.html). A quiet "Full archive" link leads to the new **archive page at /photography/** (photography/index.html): all 14 photos, single editorial column, same nav/footer/tokens, lightbox working, structured as `archive-group` sections so it can split into named projects later. Nav "Photography" now points to the archive page; the /#photography anchor still works.
- [x] **Multi-page pivot** 2026-08-08 — reference **biancacensori.com** (user's call): "nothing on the home page, everything linked in its own page, keep it minimal". Home is now a bare index (name + meta line + five links, middot field as the one effect); work split into **/photography/ · /apps/ · /projects/ · /cv/ · /info/**. The footer band is gone site-wide; /info/ carries portrait + 3-line bio + email + phone + App Store link + ©. All old one-page anchors forward via a JS map. The curated home photography selection and the hidden Music block were dropped in the rewrite (both recoverable from git history; Music could become /music/ later). Supersedes the one-page structure in [[Projects/Portfolio/site-structure|site-structure]]. **Same day: the middot field deleted entirely** ("even more minimal") — the site now has zero effects; details in [[Projects/Portfolio/design-principles|design-principles]]. The "Barcelona · open to creative roles in Europe" meta line was then cut too, and then the **Info row was removed from the index and all navs**: home is the name + four links (Photography · Apps · Projects · CV). The /info/ page still exists unlinked (old /#contact and /#about links forward there); **contact now surfaces only via the CV letterhead** (email + phone)
- [ ] **Build the site** — skeleton built 2026-06-13 at `/Users/agomezu/Claude/portfolio/` (plain HTML/CSS/JS). Design: minimal editorial, warm paper + ink, Fraunces + Inter (since 2026-08-08), name **Ale Gómez**. Pending: real screenshots, photos, portrait.
- [x] **Publish** — live at **https://alegomez.studio** since 2026-06-13 (GitHub Pages, repo: https://github.com/blhdes/portfolio, domain via Porkbun). Details in [[Projects/Portfolio/domain|domain]].
- [x] ~~Add fashion brand section at launch~~ **Cancelled 2026-08-08** — the user removed every trace of the brand from the site (BLHADES project entry, bio mention, logo asset deleted; recoverable from git). The brand stays off the site unless he decides otherwise. Same day: **Download PDF button removed** from /cv/ (print styles kept, Cmd+P still works). The CV entries were briefly stripped of their logo marks for a decluttered look, but the **user asked to keep the logotypes — "it was looking nice"** — so all 9 ink-silhouette marks (company logos + the personal square for freelance/own-practice roles) are back; typography sizes stayed smaller/quieter. Also: home page's **big hero name replaced by the same quiet top nav bar the subpages use** (name only, no right-side menu since there's nothing to link from home) — the page body now holds only the four-link index, nothing else above it. **Section headers removed from every subpage** (Photography, Apps, Projects, Info): the visible title + one-line note above each page's content is gone, replaced by a visually-hidden `<h1>` (a `.sr-only` CSS utility) so pages keep exactly one real heading for screen readers/SEO without showing anything — "let the content speak for itself." The CV page's letterhead (name + contact) is untouched, that's document content, not decorative page furniture. **All browser-tab titles now use the full legal name in caps, no section prefix**: every page — home and all subpages — shows the identical `ALEJANDRO GÓMEZ` (the `SECTION · ` prefix tried briefly, then dropped same day per user). **Fixed a nav-shift bug**: home (short, no scrollbar) vs subpages (tall, scrollbar) made the fixed top nav visibly jump sideways on navigation — fixed with `scrollbar-gutter: stable` on `html`, which reserves scrollbar space on every page regardless of content height.

**Reference revisited (biancacensori.com) for scale/masthead** 2026-08-08 — (1) overall type scale brought down (CV letterhead, app/project titles, index links, archive group titles); (2) nav masthead grew relative to nav links, text changed to **ALEJANDRO GÓMEZ** everywhere (nav and titles). **Still too big, second pass same day**: user said font size remained too big AND didn't like "the header font" (Fraunces, the editorial serif). **Fraunces removed entirely — the site is now single-typeface, Inter only**, hierarchy carried by weight/size/case, not a second display face (Google Fonts request dropped too). Sizes cut further across the board: body 17px&rarr;16px, CV letterhead name ~44px max&rarr;~28px max weight 600, nav masthead ~28px max&rarr;~20px max weight 600 (contrast vs nav links now comes mostly from weight+case, not size), app/project titles 20px&rarr;17px, CV role/org/note and archive-group-title all stepped down proportionally. Print CV name override adjusted to match (1.5rem&rarr;1.25rem).

**Nav bar given more breathing room** 2026-08-08 — user asked to look at how biancacensori.com handles its top nav's margins and apply something similar for a cleaner stand. Couldn't pull exact pixel values (Chrome browser tool wasn't connected in-session; text-fetching a live page can't read computed CSS), so this was an editorial judgment call rather than a pixel match: `.nav` vertical padding 1.25rem&rarr;1.75rem, `.nav` gap and `.nav-links` gap both 1.25-1.5rem&rarr;2rem, a touch of letter-spacing added to nav links. Mobile breakpoint bumped proportionally (still tighter than desktop). Because the bar got taller, the two places that reserve clearance for it were bumped too: `scroll-padding-top` 5rem&rarr;5.5rem and home's no-scroll top padding 5rem&rarr;5.5rem, so nothing sits behind it. **Reference re-check, resolved via screenshot** — user pasted an actual screenshot of biancacensori.com's nav, which was the real unlock: the reference **stacks** the masthead above the nav links (both sharing one left edge), it's not a side-by-side row like ours was. Restructured `.nav` from a horizontal row (name left, links pushed right via margin-auto) to a vertical stack (`flex-direction: column`, both children left-aligned, `gap: 0.85rem` between the two rows) — no HTML changes needed, the markup already nests name then links as siblings. `.nav-links` internal gap tightened 2rem&rarr;1.75rem to better match the reference's tighter link spacing. Because the nav is now taller (two rows instead of one), **every nav-clearance value across the site was bumped**: `scroll-padding-top` and all of `.index`/`body.home .index`/`.page`/`.archive`/`.cv-page`'s top padding minimums went from ~6.5-7rem to 7.5rem, so nothing sits behind the taller bar anywhere on the site.

**Index link centering corrected** — the previous pass (`align-items: center` on `.index-links`) centered each link individually, which broke their shared left edge since "Photography" and "CV" are different lengths — user caught this: "center the section text of indexes, but not the text itself, left-aligned it's good as it was." Fixed properly: `.index-links` reverted to `align-items: flex-start` (links share one left edge again) plus `width: fit-content; margin: 0 auto;` so the whole block shrink-wraps to its longest line and *that* gets centered on the page — block centered, text inside still left-aligned relative to each other. Works identically whether the parent is a plain block (Photography's `.index`) or a flex column (home's `body.home .index`), since flex auto-margins centre in both contexts.

**Nav name is now the full legal name, links justified to match its width exactly** 2026-08-08 — masthead text everywhere changed from "ALEJANDRO GÓMEZ" to **"ALEJANDRO GÓMEZ URREA"**. User then asked for the links row below it to be "perfectly padded between its edges, do not overflow after it or be shorter than it" — i.e. the nav-links row must span exactly the masthead's own width, no more, no less. Implemented via a CSS Grid technique: `.nav` is `display:grid; grid-template-columns: max-content;` (single column sized by whichever child contributes intrinsic width), `.nav-links` gets `width: 0; min-width: 100%;` (the `width:0` removes its own content width from the grid's sizing calculation, leaving `.nav-name` as the sole determinant of the column's width; `min-width:100%` then stretches nav-links to fill that exact width) plus `justify-content: space-between` so the first/last link land exactly on the masthead's left/right edges. `overflow-x:auto` stays as a safety net for any viewport where the link text genuinely can't fit. Fixed `gap` on `.nav-links` removed (no longer meaningful once space-between is in charge of spacing).

**Sticky footer, site-wide, then simplified** — user caught that on short-content pages the copyright footer sat mid-screen with empty space below it, instead of pinned to the bottom. First pass used the classic flex sticky-footer pattern (`body{min-height:100vh;display:flex;flex-direction:column}` + `flex:1` on `.page`/`.archive`), but the **same day the user reported the footer landing at inconsistent heights across different short pages** — flex-grow distribution on a container sized only by `min-height` (not a definite `height`) is more fragile than it looks. **Simplified to match `.index`'s already-proven technique**: `.page` and `.archive` now just get `min-height: 100vh` directly (same as `.index` always had), no flex involved for these three — the footer, right after in normal document flow, can never end above one full screen. Removed the now-pointless `body` flex wrapper and `.site-footer`'s `flex-shrink:0` for the general case. `body.home` keeps its own separate, still-necessary flex setup (it needs an *exact* single-screen height with `overflow:hidden`, which plain `min-height` can't do) — untouched, still correct.

**CV Experience descriptions tightened** — user: "shorten some descriptions, make it more minimalist and easy-to-read." Four of the five `.cv-note` entries trimmed (Digital product developer, Content manager, Film collaborator, Administrative receptionist); Photographer's note was already short enough and left alone. Facts kept, wording cut — e.g. Film collaborator went from two full sentences to "Festival correspondent (Venice, San Sebasti&aacute;n) and D'A programmer. Shoot assistant, DOP on shorts, screenwriter at Rodar y Rodar."

**Typeface reverted back to SF Mono / JetBrains Mono, site-wide** 2026-08-08 — user: "let's recover our apple font + fallback for the entire portfolio." `--font-body` (the site's only font variable now) changed from Inter back to `ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace` — the original 2026-07-11 decision, restored after the Fraunces+Inter detour and the Inter-only simplification. Google Fonts import switched from Inter to JetBrains Mono across all 8 pages; weight 700 added to the loaded set (previously only 400/500/600) to support bolder masthead + CV text (below). **Superseded 2026-08-19** by the Helvetica Neue / Adobe Fonts switch below.

**Home simplified: no more `.index` wrapper, `.index-links` IS `<main>`** — user: "delete the index for the home and leave only the linked menu, it's the same but in a less is more way." One fewer nesting level: `<main class="index"><nav class="index-links">...</nav></main>` &rarr; `<main class="index-links" id="top" aria-label="Sections">...</main>`. A new `main.index-links` CSS rule carries the page-width cap + nav clearance that `.index` used to provide, scoped narrowly enough that Photography's index (still `.index` wrapping a nested `<nav class="index-links">`) is completely unaffected. **Superseded same day, see below.**

**Home's separate link list removed entirely — nav now identical to every other page** — user clarified their actual intent from the change above: "let's get rid off the linked menu and inherit the default sub masthead menu like all other pages with no one section in bold because we are in home." Home's nav gained the standard `.nav-links` row (Photography/Apps/Projects/CV, same as Apps/Projects/CV/Photography's own navs), with **no `aria-current="page"`** on any of the four (home isn't one of those sections, so none should read as "active"). The vertical link-list body content is gone — since every page's nav already reaches all four sections, a second copy of the same links in home's body was redundant. Home's `<main>` is now just `.page` (the same generic wrapper Info/Projects use) holding only an sr-only `<h1>Ale Gómez</h1>` — visually, the page is nav + empty space + footer. The now-unused `main.index-links` CSS rule from the prior structure was removed; `.index-links` (base rule) is used by Photography's index only now.

**Masthead bolder** (700, up from 600) and **body text smaller/cleaner** (body font-size 1rem&rarr;0.9375rem, line-height 1.6&rarr;1.55) — this second change cascades to every paragraph that inherits body's size directly (Info's bio, Apps/Projects descriptions, CV's skills/languages/hobbies), since none of them set their own font-size.

**CV icons deleted for good, typography redesigned to compensate** — earlier in the session the user asked to keep these ([[cv-logo-marks]] memory said so); now explicitly reversed: "let's delete all icons from our CV... make it bolder, beautiful" since it "does not have any visual icons" to lean on anymore. All 9 `.cv-logo`/`.cv-self` marks removed from the HTML, the now-dead CSS (mask rules, print color-adjust override) removed too. Compensating typography changes: `.cv-role` 1rem/600&rarr;1.125rem/700 (the heaviest weight now loaded), letter-spacing tightened-negative &rarr; neutral (negative tracking suits a proportional face, not monospace, where characters are already fixed-width), `.cv-org` gained its own weight 500 (was inheriting 400, blended into the note below it), entry spacing (`.cv-item + .cv-item`) opened up 2.25rem&rarr;2.75rem since entries no longer have a mark to visually separate them. **The [[cv-logo-marks]] memory needs updating/retiring** — its "keep these" guidance is now stale and should not be revived without the user asking again.

**"Fit-screen" no-scroll treatment generalized beyond home** — user: "integrate the footer as we do to our home in all the half-empty pages like photography index or projects... always just one page if the content does not occupy all the screen." Renamed `body.home` &rarr; `body.fit-screen` (CSS and HTML both) and the inner selector generalized from `.index`-only to the bare `main` element, so one shared rule works across whatever content class a page uses. Applied to the pages confirmed short enough not to clip: home, `/photography/` (index), `/projects/`, `/info/`, `/photography/portraits/`. **Deliberately NOT applied** to `/apps/` (portrait screenshots likely taller than one screen on typical monitors) or `/photography/flash-magazine/` (13 large photos, unambiguously long) — forcing `overflow:hidden` there would clip real content, so they keep normal scrolling with the plain `min-height:100vh` treatment instead.

**Palette replaced: pure white/black, no more warm paper** 2026-08-08 — user: "i don't like the off-white colour approach, can we make it just pure white and change the palette to something really minimalistic and basic." All five tokens changed to neutral pure black/white/grey, light and dark: `--ink` #1A1815&rarr;#000000 (dark-mode #ECE9E2&rarr;#FFFFFF), `--paper` #F7F5F0&rarr;#FFFFFF (dark #181613&rarr;#000000), `--paper-warm` (CV band) #EFECE4&rarr;#F2F2F2 (dark #1F1C18&rarr;#141414), `--ink-soft` #55504A&rarr;#666666 (dark #9C968B&rarr;#999999), `--line` #D8D3C8&rarr;#E0E0E0 (dark #312E28&rarr;#2A2A2A). Also updated: the lightbox close button's hardcoded color, and all 16 `theme-color` meta tags (light+dark × 8 pages). The CV's print stylesheet already used pure black/white/grey independently — untouched, it was already aligned with this direction. CSS variable **names** (`--paper`, `--ink`, etc.) were kept as-is, only values changed, to avoid a large low-value rename across the whole file.

**Nav background removed** — `.nav` no longer paints `var(--paper)` behind it; the fixed bar is fully transparent now, so page content scrolling underneath merges naturally instead of sliding behind an opaque band. Deliberate trade-off: on tall scrollable pages, content can now visually show through/behind the nav text during scroll — accepted per the user's explicit ask.

**Nav links: bolder, and the current page now visually marked** — `.nav-links a` weight bumped from inherited 400 to 500 (the "little bolder" ask). Added a "you are here" style for the active section: `.nav-links a[aria-current="page"]` gets full ink color + weight 600 (matching the masthead's own weight), standing apart from the other links' muted `--ink-soft`. `aria-current="page"` was already correctly set in the HTML on every subpage (including both Photography sub-galleries marking "Photography" as current) — it just had zero visual styling attached until now.

**Favicon deleted (temporary)** 2026-08-08 — after the recolor + Safari-fallback + cache-bust work still wasn't visibly resolving for the user, they said to just delete it everywhere "for now". All `<link rel="icon">` / `<link rel="apple-touch-icon">` tags removed from all 8 pages; the source image files (`favicon.svg`, `.ico`, PNGs) were left on disk, untouched, since "for now" implies this comes back later — don't regenerate/re-add without being asked.

**Home + Photography index alignment fixed** 2026-08-08 — user liked an accidental side-effect (the page reading as more "centered" once the footer's copyright text got `text-align:center`) and asked to make it intentional: index links are now genuinely **horizontally centered** (`.index-links` `align-items: flex-start` &rarr; `center`, a shared rule so this applies to every page reusing `.index`/`.index-links`, not just home) but **positioned near the top of the screen, not vertically centered** (home's `body.home .index` `justify-content: center` &rarr; `flex-start`; the base `.index` top padding also tightened from `clamp(7rem,24vh,15rem)` to `clamp(6.5rem,14vh,9rem)` so Photography's index matches home's positioning). Applies today to home and `/photography/`; any future index-style page should reuse the same classes to inherit this automatically.

**Favicon recolored off vermilion, then fixed for real** 2026-08-08 — the favicon was still the old accent (`#d9432b`). `favicon.svg` flips with system theme via `@media (prefers-color-scheme: dark)` (near-black `#1A1815` light / near-white `#ECE9E2` dark, the `--ink` tokens). Raster fallbacks (`favicon-16.png`, `favicon-32.png`, `favicon.ico` now multi-res 16/32/48, `apple-touch-icon.png` solid no rounding) fixed at the light-mode color. **First pass was incomplete** — user reported still seeing vermilion. Root cause, found in this repo's own history (commit 32493bd, 2026-06-14): **Safari ignores `<style>` blocks inside SVG favicons entirely** (renders blank without a fallback), and **favicons get stuck in a browser-level cache that plain file changes don't bust** — that old commit's fix was exactly this, with `?v=2` query strings. Reapplied both fixes: `favicon.svg`'s `<rect>` now also carries a direct `fill="#1A1815"` attribute as the Safari-safe base color (CSS in `<style>` still overrides it in browsers that support the media query), and all three favicon links (`favicon.ico`, `favicon.svg`, `apple-touch-icon.png`) carry `?v=2` across all 8 pages. **Lesson for next time: always version-bust favicon links on any icon change, and never rely solely on `<style>`/CSS inside a favicon SVG** — ship the base state as a presentation attribute too.

**Download PDF restored + site-wide copyright footer** 2026-08-08 — the CV's "Download PDF" button (window.print(), CSS hidden on the printed page itself) is back on the letterhead. **A quiet `&copy; Ale Gómez, [year]` footer now closes every page except the CV** (home, Photography index + Flash Magazine + Portraits, Apps, Projects, Info) — new shared `.site-footer` CSS class, `id="year"` kept live by the existing main.js year-update code. Info's old inline copyright line (previously sitting inside the bio text) was removed in favour of this shared footer so the page doesn't carry two. CV excluded on purpose: it closes with its own letterhead/print button, not a site footer. Note: on home and the Photography index, `main.index` is `min-height:100vh`, so the footer sits just below the first screen rather than immediately visible — matches the pages' existing "vertically settled" feel, flagged in case it should change later.

**Photography split into an index + two galleries** 2026-08-08 — /photography/ is now a link index (reuses the exact `.index`/`.index-links` CSS classes the home page defines, so a style change to one applies to both, per user's explicit ask) pointing to **/photography/flash-magazine/** (13 photos: all the Flash&nbsp;Magazine editorial spreads, plus `brau-cook-free.webp` grouped in here as a judgment call — user should confirm/move it, it's a restaurant campaign not a magazine spread) and **/photography/portraits/** (1 photo, Pep Munn&eacute;; structured to take more portraits later, same `archive-group` pattern as flash-magazine). All 14 original photos accounted for, same editorial single-column `.archive` layout and lightbox as before, just split across three URLs instead of one.

**Mobile UX audit + fixes** 2026-08-08 — user reported "a bunch of UX errors" on phone. Code-level audit (Chrome extension unavailable in-session) traced the CSS against real mobile widths and found: (1) `.nav-links` is pinned to `.nav-name`'s exact intrinsic width via a CSS Grid technique, with near-zero slack specifically on mobile (masthead font shrinks to its clamp() minimum there while nav-link font stays fixed) — real device font metrics (SF Mono vs. JetBrains Mono) could tip the links past that width, and since the overflow safety net hides its own scrollbar, a link (often CV) could vanish with no visible way to reach it; fixed with `flex-wrap` inside the existing 600px breakpoint so an overflowing link drops to a second line instead. (2) Nav/index/CV-print links had no padding, well under the ~44px touch-target guidance; added an invisible `::after` hit-area expansion (no visual/layout change). (3) The lightbox's scroll-lock used `body.style.overflow = "hidden"`, which iOS Safari ignores for touch-driven rubber-band scroll; replaced with the standard position:fixed + scroll-offset-restore pattern in `main.js`. Committed as `a87738a`.

**Broader UX audit + fixes** 2026-08-08 — user asked "what more can we improve," full site audit against UX heuristics (nav, IA, a11y, performance, SEO/sharing, conversion path), cross-checked against [[Projects/Portfolio/design-principles|design-principles]] so nothing already-rejected (hairlines, effects, icons) got re-proposed. User picked 4 of 9 findings to act on: **(1) Open Graph + Twitter Card metadata** added to every page — shared site portrait (`/photography/ale-portrait.webp`) as the preview image, each page's existing meta description reused as og:description, so links pasted into outreach emails/Slack/LinkedIn now show a real card instead of a bare URL (previously nothing was set — directly relevant to the upcoming Phase 4 outreach). **(2) Custom `/404.html`** added at the repo root so a broken/mistyped link gets an on-brand page (matches nav/footer/type system) instead of GitHub Pages' bare default. **(3) CV positioning line** — one quiet line under the letterhead ("Creative from Barcelona, trained in cinema. Eight years of editorial photography, now designing apps and shipping them with AI.") so the cinema&nbsp;&rarr;&nbsp;apps&nbsp;&rarr;&nbsp;photography mix reads at a glance instead of only inferable from five job entries. **(4) `/info/` deleted outright** (user's explicit choice over relinking it or folding it into CV) — it hadn't been reachable from any nav since the 2026-08-08 multi-page pivot, just dead content; `#about`/`#contact` old-link redirects in `main.js` now point to `/cv/`, which already carries name, the new tagline, and contact. Remaining findings not actioned (noted for later if revisited): favicon still intentionally off, no robots.txt/sitemap.xml, no skip-nav link, README.md still describes the old one-page structure. Committed as `820209d`.
**Site goes bilingual: EN + ES** 2026-08-18 — user's ask: "a double language for the portfolio, with a switching option somewhere at the right of the desktop view, and somewhere optimal for the small devices responsiveness", switcher styled minimally as `EN | ES`. Implemented as **path-based localization**: every public page now has a Spanish mirror under `/es/` (`/es/`, `/es/photography/` + both galleries, `/es/apps/`, `/es/projects/`, `/es/cv/`), so picking a language keeps the *whole* nav in it rather than translating only the landing page. Chosen over a JS/localStorage toggle or auto-redirect by `Accept-Language` because real URLs stay shareable and survive being pasted into an outreach email. Nav labels (Fotograf&iacute;a / Proyectos), all app + project descriptions, and the **full CV** (roles, orgs, notes, education, skills, languages, hobbies) are translated. Pages pair via `hreflang` (en / es / x-default) so the two read as one site, not duplicate content. **404 is deliberately bilingual rather than paired** — a broken link could belong to either tree and GitHub Pages only ever serves the one root `404.html`. **Oposiciones pages excluded** (already Spanish, unlinked, separate track) but their `?v=` was bumped like every other page. Switcher CSS: `position:absolute` top-right of the fixed `.nav` on desktop; on phone it goes `static` and becomes a quiet third row under the section links, since the masthead can run close to full width there. **Trade-off accepted and worth remembering: content now lives in two places** — every future edit (a new app, a new CV line) must be made in both the EN page and its `/es/` mirror, or they drift.

**Mobile nav links no longer overrun the masthead** 2026-08-18 — user caught that on phone the links row edged out past the masthead's right edge, while desktop matches it exactly. Cause: the mobile media query threw away the desktop width-matching (`grid-template-columns: max-content` + `.nav-links`' `width:0/min-width:100%` pair) in favour of its own full-bleed layout (`1fr` + `justify-content:flex-start` + a fixed `1.25rem` gap), which measured ~276px against a ~219px masthead. That override existed for a real reason — at the inherited `0.875rem` the four links alone are already about as wide as the masthead, so matching its width left ~1.5px of gap. Fix: restore the desktop technique on mobile **and step the link text down to `0.75rem`**, which frees ~35px to distribute, so the row lands flush with the masthead's edges *and* keeps real gaps. Touch targets unaffected (they come from the invisible `::after` hit-area expansion, not font size); the `flex-wrap` safety net stays. Verified pixel-exact (`edge delta L=0.0 R=0.0`) on all 8 pages, EN and ES.

**Mobile nav links made legible again, via masthead tracking** 2026-08-18 — the fix above shrank the links to `0.75rem` to make them fit; on a real phone the user found that too small ("was better/more legible before") and asked to think harder. The constraint is **geometric**: the four links total ~25 characters and must sit inside a masthead only 21 characters wide, whose font is pinned at its `clamp()` minimum (1.0625rem) on phone — at a legible 0.875rem they fill the row almost exactly, leaving ~1.4px between words. So the extra width has to come from the masthead. Three options were measured inside a fixed-width iframe and offered to the user: **(A) track out the masthead** (font unchanged, `letter-spacing` 0.01em&rarr;0.09em, +28px, gaps 10.9px), (B) grow the masthead 1.0625&rarr;1.1875rem (gaps 10.0px), (C) masthead at the full desktop 1.25rem (gaps 14.2px). **User chose A** — and it's the right instinct given how many passes were spent bringing type sizes *down*: the masthead keeps the exact size he tuned, only its tracking opens, and tracked-out caps suits an editorial masthead so it reads as intent rather than a workaround. Links back at 0.875rem. Verified flush at 375px and 320px on all 17 pages. **Pattern worth reusing: when two rows must share a width, the fix can come from tracking instead of font size** — it changes the measure without touching the type scale.

**Language switcher moved to the page foot on phone** 2026-08-18 — user: "for phone screens i prefer to see the language switcher at the bottom of the screen", then clarified "like just before the footer" (i.e. **above** the copyright line, not below it). The third-row-under-the-nav placement made the nav read as three stacked menus for a control that gets used once, if ever. **Deliberately in flow, not fixed to the viewport**: the switch has no background of its own, so pinning it to the screen would leave floating text over whatever scrolls underneath — the same objection that took the nav off `position: fixed` on mobile. Sitting in the footer means it lands at the true bottom of the screen on pages that fit one (home, photography index, portraits) and at the end of the content on pages that scroll. Because "just before the footer" is a *flow* position, CSS alone couldn't do it — the **markup moved** from `<nav>` into `<footer class="site-footer">` on the 12 pages that have one, which in turn meant desktop switched from `position: absolute` to **`fixed`** so it keeps its top-right spot regardless of document order. **The two CV pages have no footer by design**, so there it closes `<main>` instead; and `.lang-switch` had to join the print stylesheet's hidden list, since hiding `.nav` no longer covers it. Cache-bust `v=78`.

**Full name everywhere, page and metadata** 2026-08-18 — the footer copyright now reads `&copy; Alejandro G&oacute;mez, <year>` instead of "Ale Gómez", matching the masthead. Applied to all 15 pages that carry a footer (the two CV pages have none by design). No `?v=` bump, since no CSS or JS changed. **Then extended to the sharing metadata**: the user asked for consistency after (mistakenly, see the caching entry below) thinking the ES footers had been skipped — the footers were fine, but the check turned up **42 metadata tags still carrying the short name** (`og:site_name`, `og:title`, `twitter:title`, all the meta descriptions) plus the two hidden `sr-only` `<h1>`s on the home pages. Invisible on the page, but `og:*` is exactly what renders when a link is pasted into an email or LinkedIn, so **a shared link previewed under a different name than the page it opened** — directly relevant to Phase 4 outreach. User chose to align them. The site now reads "Alejandro Gómez" everywhere except the CV letterhead ("Alejandro Gómez Urrea") and the nav masthead (same, in caps). **Worth remembering: "make the name consistent" is not just the visible copy** — check `og:`/`twitter:` tags, meta descriptions, `sr-only` headings and `alt` text too.

**Gap in the cache-busting convention: `?v=N` does nothing for HTML** 2026-08-18 — after the switcher moved to the footer, the user reported `/es/` "still shows the legacy language switcher". The deploy was actually fine (`curl` confirmed `v=78` and the switcher in the footer on the live site); what they were seeing was a **stale cached copy of the HTML document** on their phone. The `?v=N` trick busts `style.css` and `main.js` because the query string is part of *their* URLs, but the HTML pages themselves carry no query string, so a browser or CDN can keep serving an old page even when the CSS is fresh. This never bit before because changes were nearly always CSS-only; it surfaced now because the switcher's new position is a **markup** change. **How to handle it:** when a change is structural (markup moved, elements added/removed) rather than styling, expect a lag on already-visited pages, verify the deploy with `curl` rather than trusting a phone reload, and tell the user to close the tab / use a private tab instead of assuming the push failed. See [[portfolio-cache-busting]].

**Lesson: headless Chrome screenshots on macOS lie below ~500px** 2026-08-18 — while verifying the above, a `--window-size=375,900` screenshot appeared to show the CV page's text overflowing its right edge, and this was briefly reported to the user as a real mobile bug. It wasn't: macOS enforces a **minimum window width around 500px**, so Chrome renders at ~500 CSS px and the screenshot merely *crops* to the requested width — cut-off text is a cropping artifact, not overflow. Caught by injecting a probe script that printed `document.documentElement.clientWidth` into the page (it read **500**, not 375). **The reliable technique for narrow-viewport testing: load the page in a fixed-width `<iframe>`** (an iframe gets its own true viewport) and measure inside it via same-origin script — that reported a genuine `viewport=375`, `scrollWidth=375`, `0 overflowing elements` on every page. Use the iframe harness, not `--window-size`, for any future mobile check; and don't report a layout bug from a small-window screenshot without confirming the real viewport first.

**That harness is now a permanent script: `scripts/check-layout.sh`** 2026-08-18 (user's request, "save it as a documented script inside the project"). Run `./scripts/check-layout.sh` for every page at 375px, or pass a width and optional paths (`./scripts/check-layout.sh 320 /cv/`). It reports horizontal overflow (naming the offending elements) and whether the nav links row is still flush with the masthead, exits non-zero on failure, and **discovers pages with `find`** so new sections and future `/es/` mirrors are covered without editing it. Results come back as text via Chrome's `--dump-dom` (the harness writes its findings into the DOM), so it's a real pass/fail check rather than something to eyeball. **A second trap found while testing it:** the nav check must measure a **`Range` over each element's contents, not the element boxes** — grid items stretch to fill their column, so with the old full-bleed mobile nav both boxes reported identical widths while the text inside ended ~56px apart, and a box-based check passed the very bug it was written to catch. Confirmed by reverting the fix and watching the box version pass and the Range version fail at 55.8px. Documented in the script's header and mentioned in `README.md`.

**Typeface switched to Helvetica Neue via Adobe Fonts, monospace retired for good** 2026-08-19 — user noticed the site's font had a real fallback on Windows (JetBrains Mono, since Apple won't license SF Mono for web use off-Apple hardware) and asked whether Helvetica Neue could be used everywhere instead, via the Adobe Creative Cloud account. Weighed against the cheaper fix (serve JetBrains Mono to every OS, zero cost, keeps the monospace identity); **user chose the Helvetica Neue pivot**, ending the monospace identity that had been chosen 2026-07-11 and reaffirmed 2026-08-08 (see the superseded entries above). Adobe Fonts confirmed **Helvetica Neue LT Pro** (Monotype, 14 styles) available on the account, unlimited pageviews/sites; created a dedicated web project **`alegomez-portfolio`** (id `pbq8oze`) with just the 3 weights the site actually uses — Roman/400, Medium/500, Bold/700, no italics, no condensed — embedded via `<link rel="stylesheet" href="https://use.typekit.net/pbq8oze.css">` on all 17 pages (replacing the Google Fonts JetBrains Mono `<link>`), `--font-body` now `"helvetica-neue-lt-pro", "Helvetica Neue", Helvetica, Arial, sans-serif`. **Real font-matching gotcha found along the way:** Adobe's Helvetica Neue has no 600/Semibold, only 400/500/700 — every `font-weight: 600` in the CSS (section titles, app/project titles, study-block titles) was remapped to **500**, except the two "current state" indicators (nav `aria-current="page"`, the active EN/ES lang-switch link) which went to **700** instead, since their own comment explicitly called for reading "as bold as the masthead itself" — collapsing them to 500 would have made the active state visually identical to inactive links. Verified with `scripts/check-layout.sh` at 320px and 375px (all 17 pages pass, nav-to-masthead width-matching held up fine since it measures live text width rather than assuming metrics) plus a visual pass on home/CV/apps. Cache-bust `v=79`. **Trade-off accepted:** this now depends on the Adobe Creative Cloud subscription staying active — if it lapses, `use.typekit.net` stops serving and the stack falls back to the OS's own Helvetica Neue/Helvetica/Arial, which is a graceful (if no-longer-identical) degradation rather than a break.

- [ ] **Job search & relocation** — workstream started 2026-06-14, see [[Projects/Portfolio/Job-Search/Job-Search|Job-Search]]. **Pivot 2026-07-11: local-first** (Barcelona/Sant Cugat + Madrid) to earn & save; abroad stays the mid-term goal. **2026-07-11: 202 companies with direct-contact emails** collected in [[Projects/Portfolio/Job-Search/outreach-directory|Outreach Directory]] — next: user skims and shortlists. **2026-07-27: first outreach drafted** (Division Global, Paris — internship address) ahead of the local-first sequencing, user's call; portfolio confirmed shareable v1 so the send gate holds. **2026-08-08: new industry vertical — adult film / erotica**, user's own request, seeded with Erika Lust Films (Barcelona); Madrid scaffolded empty.

## Related

- Job search: [[Projects/Portfolio/Job-Search/Job-Search|Job-Search & Relocation]]
- App projects: [[Projects/Culla/Culla|Culla]], [[Projects/Doppio/Doppio|Doppio]], Warket, Village
